import java.util.*;
import java.util.regex.Pattern;

public class TimeAndTimeAgain
	{
		private static Scanner input = new Scanner(System.in);
		private static Pattern secondRegex = Pattern.compile(".*\\d+)s.*");
		private static Scanner intGetter = new Scanner(System.in);

		public static void main(String[] args)
			{
				System.out.println("Hello World");
				int numTimeCases = intGetter.nextInt();
				// Format needed HH:MM:SS
				// Read in input
				// First number is the number of items

				// Loop through content
				for (int i = 0; i < numTimeCases; i++)
					{
						
					}

				// Print out the numbers in appropriate format
			}

	}
